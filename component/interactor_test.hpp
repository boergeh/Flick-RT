#include "interactor.hpp"
namespace flick {
  begin_test_case(interactor_test) {
    //lambert_surface_interactor lsi(1,0);
    //pose p{{0,0,0},{1,0,0}};
    //lsi.radiation_package(radiation_package{p,{1,0,0,0}});
    //lsi.normal({1,0,0});
    //std::cout << lsi.leaving_radiation().pose().z_direction();
    //check(lsi.leaving_radiation().pose().z_direction().x() < 0);
  } end_test_case()
}
